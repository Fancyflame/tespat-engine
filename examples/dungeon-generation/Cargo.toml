[package]
name = "dungeon-generation"
version.workspace = true
edition.workspace = true

[dependencies]
rand.workspace = true
tespat = { version = "0.1.0", path = "../../tespat-core" }
log.workspace = true

[build-dependencies]
tespat-build = { path = "../../tespat-build" }
