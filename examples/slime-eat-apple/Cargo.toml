[package]
name = "slime-eat-apple"
version.workspace = true
edition.workspace = true
publish = false

[build-dependencies]
tespat-build = { version = "0.1.0", path = "../../tespat-build" }

[dependencies]
tespat = { version = "0.1.0", path = "../../tespat-core" }
